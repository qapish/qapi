// NOTE: Placeholder. Replace with metadata-derived decoding using @qapish/runtime
export function decodeHeader(_bytes: Uint8Array) {
  // Return a minimal shape to unblock API surface
  return { number: 0, parentHash: "0x", stateRoot: "0x", extrinsicsRoot: "0x", digest: [] as any[] };
}
